{
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y cmake make gcc && git clone https://github.com/ashinn/chibi-scheme.git /tmp/chibi && cd /tmp/chibi && make && sudo make install"
}
